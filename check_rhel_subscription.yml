---
- hosts: all
  gather_facts: false
  tasks:
    - command: /usr/bin/subscription-manager status
      become: yes
      register: result
      failed_when: result.rc >= 2
      changed_when: False
    
    - debug:
        msg: "{{ result }}"
